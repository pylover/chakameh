#:import MediaArt chakameh.uix.media_art.MediaArt
#:import ArtBox chakameh.uix.media_art.ArtBox
#:import config chakameh.config.config

<ArtBox>:
  # direction: 'right'
  # AsyncImage:
    # source: config.arts.noimage
  # AsyncImage:
    # source: config.arts.noimage
  # AsyncImage:
    # source: config.arts.noimage
    
#  pos: root.x, -(self.height - root.height)

        
[ArtBoxTitle@StencilView]:
  size_hint_x: None
  width: 70.0 * ctx.item.collapse_alpha
  Image:
    source: config.arts.noimage
    size: 800,600
    pos: root.x, -(self.height - root.height)

<MediaArt>:
  Accordion:
    min_space: 72
    ArtBox:
      category: 'artist'
    ArtBox:
      category: 'composer'
    ArtBox:
      category: 'lyricist'
    
